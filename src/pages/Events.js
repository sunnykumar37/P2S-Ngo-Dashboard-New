import React from 'react';
import { Typography, Box } from '@mui/material';

function Events() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Events Management
      </Typography>
      <Typography variant="body1">
        Plan and manage events here.
      </Typography>
    </Box>
  );
}

export default Events; 